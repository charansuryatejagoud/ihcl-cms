import { FeatureSchemaDefinition } from "schemas/types";

export const details: FeatureSchemaDefinition = {
    variants: {
        group:[
            {
                title:"[Details] Group1",
                value:"details.group.group1"
            }
        ]
    }
    
}