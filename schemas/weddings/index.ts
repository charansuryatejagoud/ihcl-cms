import { FeatureSchemaDefinition } from "schemas/types";

export const weddings: FeatureSchemaDefinition = {
  variants: {
    group: [],
    card:[
      {
       title:"[Weddings] Card With Circle Image Content",
       value:"weddings.card.card-with-circle-image-content"
      }
    ]
  }
}